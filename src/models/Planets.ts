//  Planets model to display on main page

export interface Planets {
  name: string;
  rotation_period: string;
  orbital_period: string;
  climate: string;
  terrain: string;
  population: string;
}
